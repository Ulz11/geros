# Normalize line endings. Critically: shell scripts must stay LF — a CRLF
# checkout on Windows would break bash when install.sh/backup.sh land on the VPS.
* text=auto
*.sh text eol=lf
*.service text eol=lf
Caddyfile text eol=lf
*.png binary
*.jpg binary
*.exe binary
